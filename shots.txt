# README shots, rendered by the extensions fork:
#   tests/widget-shots.sh -x ~/.config/illogical-impulse/widgets/statusphere
# Every scene feeds the widget made-up members through the same `ingest` the
# cli talks to - no live account, nobody real. The plain and edge rooms
# (demo/DemoRoom.qml) are not here on purpose: they are for looking at while
# working, and `widget-shots.sh -x . -- "-f demo/DemoRoomEdge.qml -n edge -g
# 430x1180"` draws the edge one.
-f demo/DemoRoomSimple.qml -n room -g 420x620 -s 1500
-f demo/DemoCardEditor.qml -n StatusphereSettings -p shot=packs-row -g 480x480 -s 12000
-f demo/DemoFriendCard.qml -n friendCard -g 420x480 -s 1500
