# README shots, rendered by the extensions fork:
#   tests/widget-shots.sh -x ~/.config/illogical-impulse/widgets/statusphere
# The room shot runs off demo/DemoRoom.qml, which feeds the widget made-up members
# through the same `ingest` the cli talks to - no live account, nobody real.
# The edge scene is not here on purpose: it is for looking at while working, and
# `widget-shots.sh -x . -- "-f demo/DemoRoomEdge.qml -n edge -g 430x1180"` draws it.
-f demo/DemoRoom.qml -n room -g 430x700
-f StatusphereSettings.qml -g 560x740
