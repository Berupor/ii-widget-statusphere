//@ probe statusphere -g 430x1180 -s 2000
/** The same scene, fed the room where everything is too long, too many or broken. */
import "."

DemoRoom {
    scenario: "edge" // In qml, not a probe flag: shots.txt gets it for free
}
